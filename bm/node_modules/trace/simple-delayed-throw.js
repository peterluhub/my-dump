'use strict';
require('../../trace.js');

//
// test global method (setTimeout)
//

function delayed(error) {
  throw error;
}

setTimeout(function a() {
  setTimeout(function b() {
    const error = new Error('trace');
    process.nextTick(delayed, error);
  }, 0);
}, 0);
